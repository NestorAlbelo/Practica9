class Matrices
	attr_reader :ancho, :matriz
	def initialize(ancho,*elements)
	  @ancho=ancho
	  @matriz=nil
	end
	def to_s
	  @matriz
	end  
	def [](i)
		@matriz[i]
	end

	def []=(i,other)
		@matriz[i]=other
	end
	
	def comprobacion		#Comprueba si hay que transformar la matriz
	  if(self == MatricesDis)
	    while(
end